<h2 align="center">Welcome to <%= @user.username %>'s page</p>

<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%=gravatar_for @user, size: 150 %>
  </div>
</div>

  <h4 align="center"><%= @user.username%>'s articles</h4>
    <div class="center">
      <%=will_paginate @user_articles %> 
    </div>
  
    <%#=render "articles/article", obj: @user.articles %>
    <%=render "articles/article", obj: @user_articles %>
    
    <div class="center">
      <%=will_paginate @user_articles %> 
    </div>
    
    
    
  <div class="user-actions">
  
    <%=link_to 'Edit User', edit_user_path(@user), class: "btn btn-sm btn-primary" %>
    <%=link_to 'Delete User', user_path(@user), method: :delete, data: {confirm: "Are you sure?"},  class: "btn btn-sm btn-danger" %>
    <%=link_to 'Back to Users', users_path, class: "btn btn-sm btn-success" %>
  </div> 
</div>

